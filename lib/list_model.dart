class ListModel {
  int id;
  String name;
  String logo;
  String message;
  DateTime dateTime;
  bool isHead;

  ListModel(
      this.id, this.name, this.message, this.logo, this.dateTime, this.isHead);
}
